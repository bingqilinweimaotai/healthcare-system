<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthcare.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.healthcare.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="status" property="status" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, phone, nickname, avatar, role, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{phone}, #{nickname}, #{avatar}, #{role}, #{status}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE sys_user
        SET username = #{username}, password = #{password}, phone = #{phone}, nickname = #{nickname},
            avatar = #{avatar},
            role = #{role}, status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE sys_user SET status = #{status}, updated_at = NOW() WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*) FROM sys_user WHERE username = #{username}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_user ORDER BY created_at DESC
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM sys_user
    </select>
</mapper>
