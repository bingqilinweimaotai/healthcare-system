<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthcare.mapper.AiSessionMapper">

    <resultMap id="BaseResultMap" type="com.healthcare.entity.AiSession">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="status" property="status" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_session (user_id, status, created_at)
        VALUES (#{userId}, #{status}, NOW())
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM ai_session WHERE id = #{id}
    </select>

    <select id="selectByUserIdOrderByCreatedAtDesc" resultMap="BaseResultMap">
        SELECT * FROM ai_session WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>
</mapper>
